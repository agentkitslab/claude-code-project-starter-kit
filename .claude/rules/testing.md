# Testing Rules

- Add or update tests for new behavior.
- For bug fixes, add a regression test that fails before the fix when practical.
- Run the narrowest relevant test first.
- Do not delete, skip, or weaken tests just to make CI pass.
- If tests cannot be run, clearly report the command attempted and the blocker.
